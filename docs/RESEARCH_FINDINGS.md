# GIZA Research Findings Registry — reviewed v0.10.6

The persistent registry contains **29 findings**. v0.10.6 adds two metric-registration research findings while preserving all previous truth states.

## Plan agreement is structured, not uniform

`finding.concordance.plan-agreement-is-structured` records that several major Khafre plan/program facts converge across distinct source families, while detailed furnishing/function reconstructions do not always converge. This is a stronger basis for prioritizing metric extraction, but it is not geometry promotion.

## Repeated topology-state transitions

`finding.visibility.repeated-state-transitions` records that the component/room graph contains repeated transitions between exterior, threshold, enclosed route, hall/court, inner-zone and closure/chamber states. This is a topology result only. It does not establish ritual meaning, visibility, emotion, or symbolic intensity.

The strongest current intent baseline remains royal mortuary transition + continuing royal cult, with staged ritual landscape additionally supported architecturally. Future-message remains `NOT_ESTABLISHED`; machine/power remains `NOT_SUPPORTED`; advanced computation remains `NO_DIRECT_EVIDENCE`.

## v0.10.6 metric boundary

Two findings were added: the procedural-architectural grammar is now the strongest pre-metric pattern to falsify, and Hölscher’s Valley Temple material/image/light reconstruction is now an explicit metric test target. Neither is promoted to fact; both require source-byte registration and independent metric tests.


## v0.10.7 review

Three findings were added:

- **Plate-local metric decoupling:** valid room-scale metric geometry does not require a solved plateau-wide GPMP translation. Local and global frame authorities can be tested separately.
- **Preregistered holdout gate:** the first archaeological plan registration can no longer be tuned after seeing residuals; model, holdout rules and thresholds are frozen first.
- **Petrie transfer failure is operational, not evidentiary:** remote source identity/licensing is verified while local byte custody remains absent. GIZA keeps both facts simultaneously instead of collapsing them.

No archaeological metric geometry was promoted.
