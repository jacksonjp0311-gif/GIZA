# GIZA Roadmap — v0.10.7

## Current — v0.10.7 // FIRST-PLATE REGISTRATION + HOLDOUT GATE

Freeze the first real registration test before source-byte fitting. The release separates plate-local metric authority from plateau geodesy and records byte-acquisition failures without promoting remote files into local custody.

## Next — v0.10.8 // FIRST REGISTERED PLATE

On a networked desktop:

- acquire/hash Petrie 1883 public-domain scan;
- visually identify Plate VI;
- freeze distributed fit controls and independent holdouts;
- run the unchanged similarity gate;
- preserve failure if thresholds are missed;
- after a passing/manual-reviewed receipt, emit first `PLATE_LOCAL_METERS` wall/opening candidates;
- enable the first intra-building metric visibility rays only for that accepted local frame.

## Then — v0.10.11 // INDEPENDENT VALLEY-TEMPLE REPLICATION

Resolve the exact ARCE/Open Context 1:100 drawing item and register it independently. Compare at least five shared architectural holdouts across east entrances, T-hall junction, west wall, north arm and south arm.

The comparison must report disagreement rather than force fit.

## Later — comparative metric grammar

Repeat the same independently registered pipeline across Pyramid Temple, Sphinx Temple, Khufu and Menkaure complexes. Only patterns surviving independent plan sources and monument holdouts should graduate into civilization-level architectural grammar hypotheses.
