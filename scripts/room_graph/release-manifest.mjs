import fs from 'node:fs';import crypto from 'node:crypto';const files=['public/model/room_graph/manifest.json','public/model/room_graph/graph.json','public/model/room_graph/spaces.json','public/model/room_graph/thresholds.json','public/model/room_graph/constraints.json','public/model/room_graph/findspot_links.json','public/model/maps/layers/room_graph.json','docs/ROOM_GRAPH.md'];const hash=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');const m=JSON.parse(fs.readFileSync(files[0]));const out={version:'0.10.7',codename:'ROOM GRAPH',phase:'ROOM_GRAPH',generated_at:new Date().toISOString(),canonical_geometry_changed:false,nexus_shell_changed:false,counts:m.counts,invariants:['room coordinates are schematic UI positions','threshold topology does not prove ritual choreography','findspot does not prove original placement','visibility remains qualitative until metric plan parsing','room graph has zero geometry-write authority'],artifacts:files.map(path=>({path,sha256:hash(path)}))};fs.writeFileSync('ROOM_GRAPH_MANIFEST.json',JSON.stringify(out,null,2)+'\n');console.log(`ROOM GRAPH manifest spaces=${m.counts.spaces} thresholds=${m.counts.thresholds}`);
