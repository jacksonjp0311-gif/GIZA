# GIZA v0.11.1 — continuation contract

Continue the actual software in this folder. Preserve the existing 3-D explorer, atlas, scientific data, source histories and original design references.

The Registration Workbench is `public/workbench/` + `scripts/workbench/`. Its shared validated engine is `scripts/plate_registration/engine.mjs`; do not fork validation rules into the UI or CLI. Run `npm run test:repairs` after changing registration or source parsing.

Do not relax profile minima, omit scale evaluation, substitute stored hashes for rehashing bytes, allow missing target provenance, or overwrite frozen experiments. Normalized observations stay normalized even when raw files happen to be cached.

Preserve `geometry_authority` distinctions. Synthetic tests are not Giza measurements. No current real archaeological fit or metric ray has been produced. Operator-supplied provenance is not independently authenticated by the application.

Before delivery, run the applicable tests, update VALIDATION honestly, then `npm run release:manifest` and `npm run verify:release`. Historical artifacts retain their original evidence/module versions; root product identity must match package.json/GIZA.json/CONTINUITY.json. Never bulk-rewrite version history or old source receipts just to satisfy string checks.

GitHub writes require explicit user authorization. On 2026-09-17 the user authorized creating GIZA, pushing this project, and delivering a repository analysis. This does not authorize unrelated future GitHub mutations.
